services:
  mongodb:
    image: mongo:latest
    container_name: mcp-mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - mcp-network

volumes:
  mongodb_data:

networks:
  mcp-network:
    driver: bridge
